<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.safety.AiSurveillanceActivity">

    <!-- Camera Preview -->
    <androidx.camera.view.PreviewView
        android:id="@+id/viewFinder"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- AI Overlay -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        
        <!-- Mock Detection Box -->
        <View
            android:id="@+id/detectionBox"
            android:layout_width="200dp"
            android:layout_height="300dp"
            android:layout_centerInParent="true"
            android:background="@drawable/bg_gradient"
            android:alpha="0.3"/> <!-- Using gradient as placeholder border since we lack shape drawable here easily without defining one -->

        <TextView
            android:id="@+id/tvDetectionLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="PERSON DETECTED (98%)"
            android:textColor="#00FF00"
            android:textStyle="bold"
            android:background="#80000000"
            android:padding="4dp"
            android:layout_above="@id/detectionBox"
            android:layout_centerHorizontal="true"/>
            
    </RelativeLayout>

    <!-- HUD -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="#80000000">
        
        <TextView
            android:text="AI SECURITY CAM"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:textSize="18sp"/>
        
        <TextView
            android:text="Analyzing live feed..."
            android:textColor="#00FF00"
            android:textSize="12sp"/>
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabCapture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="32dp"
        app:srcCompat="@drawable/ic_camera"
        app:backgroundTint="#FFFFFF"/>

</RelativeLayout>
